name: Sharp Drop Reversal Crowding Yearly
on:
  workflow_dispatch:
  push:
    paths: ['scripts/analyze_sharp_drop_reversal_crowding_threshold_yearly.py']
permissions:
  contents: write
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: pip install pandas
      - run: python scripts/analyze_sharp_drop_reversal_crowding_threshold_yearly.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/sharp_drop_reversal_crowding_threshold_yearly.json
          git diff --cached --quiet && exit 0
          git commit -m "Run reversal crowding yearly scan"
          git pull --rebase origin main
          git push
