name: Selling Climax True vs False Diagnostic
on:
  workflow_dispatch:
  push:
    paths: ['scripts/analyze_selling_climax_true_vs_false.py']
permissions: {contents: write}
jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: pip install pandas numpy requests
      - run: python scripts/analyze_selling_climax_true_vs_false.py
        env:
          JQUANTS_API_KEY: ${{ secrets.JQUANTS_API_KEY }}
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/selling_climax_true_vs_false_diagnostic.json
          git diff --cached --quiet && exit 0
          git commit -m "Run selling climax true-vs-false diagnostic"
          git pull --rebase origin main
          git push
