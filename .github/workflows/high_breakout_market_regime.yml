name: High Breakout Market Regime

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/analyze_high_breakout_market_regime.py'

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Checkout private J-Quants data
        uses: actions/checkout@v4
        with:
          repository: screamdayo/stock-screening-data
          token: ${{ secrets.STOCK_SCREENING_DATA_TOKEN }}
          path: .private-data
          sparse-checkout: |
            data/batches
          sparse-checkout-cone-mode: false
      - name: Mount private data
        run: |
          mkdir -p data
          ln -sfn "$GITHUB_WORKSPACE/.private-data/data/batches" data/batches
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install pandas pyarrow numpy
      - name: Run market regime analysis
        run: python scripts/analyze_high_breakout_market_regime.py
      - name: Commit result
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/high_breakout_market_regime.json results/high_breakout_market_regime_trades.csv
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Run high breakout market regime analysis"
          git pull --rebase origin main
          git push origin HEAD:main
